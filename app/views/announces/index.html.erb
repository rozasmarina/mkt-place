<%# navbar %>
<%# banner na home? %>
<%# add photos do cards %>
<%# what's the strcuture of the home page? %>
<div>
    <div>
    <% @announces.each do |announce| %>
        <p><%= announce.product_name %></p>
    <% end %>
    </div>
</div>


<%# footer %>
